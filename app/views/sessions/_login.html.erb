<%= form_with class: "js--login", url: sessions_path do |f| %>
  <%= f.text_field :username, placeholder: "Username"  %>
  <%= f.password_field :password, placeholder: "Password"  %><br>
  <%= f.submit "Login" %>
<% end %>

<%= render 'shared/errors' if @errors %>
