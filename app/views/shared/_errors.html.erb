<div class="error_explanation">
  <h2><%= pluralize(@errors.count, "error") %> prevented this record from being saved:</h2>
  <ul>
  <% @errors.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
</div>
